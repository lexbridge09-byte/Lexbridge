// Default plan for the WhatsApp AI assistant. The API can override these with
// WHATSAPP_AI_FREE_MESSAGES / WHATSAPP_AI_PACK_MESSAGES / WHATSAPP_AI_PACK_PRICE_PAISE;
// the website copy reads these defaults, so update both together.
export const WHATSAPP_AI_PLAN = {
  freeMessages: 5,
  packMessages: 30,
  packPricePaise: 19900,
};
